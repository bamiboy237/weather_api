# Flask framework
Flask==2.3.3  

# Template and Form handling
Flask-WTF==1.0.1

# Gunicorn for deployment (production server)
gunicorn==20.1.0

# Flask-Caching (optional, if you use caching in your app)
Flask-Caching==1.11.1

# Jinja2 for templating (automatically included with Flask, but can be explicit)
Jinja2==3.1.2

# Requests (if your app makes API requests)
requests==2.31.0

# Flask-Cors (if you need Cross-Origin Resource Sharing support)
Flask-Cors==3.0.10

# Markupsafe (for template escaping)
MarkupSafe==2.1.3

# Werkzeug (WSGI utility library used by Flask)
Werkzeug==2.3.7

# For working with .env files (optional)
python-dotenv==1.0.0
